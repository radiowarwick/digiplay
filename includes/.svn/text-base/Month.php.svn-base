<?php
class Month{
	protected $days;
	protected $year;
	protected $month;
	protected $events;
	public function __construct($year,$month){
		$this->year = $year;
		$this->month = $month;
		$this->days = array();
	}
	public function append_event($event){
		//Loop through adding events to Days. Beware of days that pass month limits!
	}
	public function load_events(){
		$result	= RawDB::query("SELECT * FROM events WHERE starttime BETWEEN x and y OR endtime BETWEEN x and y ORDER BY");
		$array	= array();
		while($object = pg_fetch_object($result,null,'ScheduledEvent'))
			$this->append_event($object);
	}
	public function get_days(){
		$daysinmonth = 0; //work this out using date
		for($i = 0; $i < $daysinmonth; $i++)
			$this->days[$i] = new Day();
		return $days;
	}
}
?>